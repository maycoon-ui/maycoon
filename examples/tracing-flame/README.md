\# `tracing-flame` example



This example demonstrates how to use `tracing` and `tracing-flame` to generate flamegraphs.



The generated `tracing.folded` can be converted into a flamegraph using `cat tracing.folded | inferno-flamegraph > tracing-flamegraph.svg`.

